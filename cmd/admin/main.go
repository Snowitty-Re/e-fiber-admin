package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "e-fiber-admin: api server bootstrap placeholder")
	fmt.Fprintln(os.Stderr, "implement per docs/01-architecture.md and docs/05-modules.md")
	os.Exit(0)
}
