package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "e-fiber-admin: asynq worker bootstrap placeholder")
	fmt.Fprintln(os.Stderr, "implement per docs/07-events-jobs.md")
	os.Exit(0)
}
